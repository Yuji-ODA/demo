package com.example.demo.lib;

import org.junit.jupiter.api.Test;

class FTest {

    @Test
    void test() {
//        var doubleInt = F.<Integer>unary(x -> x*2).andThen(String::valueOf);
//        var addInt = (Integer x) -> F.<Integer>unary(y -> x+y);
//        assertThat(doubleInt.apply(10)).isEqualTo("20");
//        assertThat(addInt.apply(10).apply(20)).isEqualTo(30);
    }
}